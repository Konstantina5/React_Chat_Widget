import { FC, Fragment } from 'react';
import { HeaderProps } from '../../types/components/Header.types';

const Header: FC<HeaderProps> = ({ /** TODO: Destructure props */ }) => {
  return (
    <Fragment />
  );
};

export default Header;
