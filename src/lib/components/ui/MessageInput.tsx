import { FC, Fragment } from 'react';
import { MessageInputProps } from '../../types/components/MessageInput.types';

const MessageInput: FC<MessageInputProps> = ({ /** TODO: Destructure props */ }) => {

  return (
    <Fragment />
  );
};

export default MessageInput;
