import { FC, Fragment } from 'react';
import { ChatWindowProps } from '../../types/components/ChatWindow.types';

const ChatWindow: FC<ChatWindowProps> = ({ /** TODO: Destructure props */ }) => {

  return (
    <Fragment/>
  );
};

export default ChatWindow;
