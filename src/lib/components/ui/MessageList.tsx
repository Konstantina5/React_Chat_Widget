import { FC, Fragment } from 'react';
import { MessageListProps } from '../../types/components/MessageList.types';

const MessageList: FC<MessageListProps> = ({ /** TODO: Destructure props */ }) => {
  return (
    <Fragment />
  );
};

export default MessageList;
